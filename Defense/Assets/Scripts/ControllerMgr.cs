using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using UnityEngine;
using UnityEngine.EventSystems;
using UnityEngine.UI;

public class ControllerMgr : MonoBehaviour
{
    void Awake()
    {
        //BuildingMgr.GetInstance().init();
        //BagManager.GetInstance().Init();
        //LoadLevelManager.GetInstance().init();
        //MonsterCreateManager.GetInstance().init();
    }

}
